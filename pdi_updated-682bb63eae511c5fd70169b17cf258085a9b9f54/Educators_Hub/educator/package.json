{
  "name": "ekya-educator-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "cd apps/web && npm run dev",
    "dev:api": "cd apps/api && npm run dev",
    "dev": "npm-run-all --parallel dev:web dev:api",
    "build": "npm run build --workspace apps/web && npm run build --workspace apps/api",
    "lint": "npm run lint --workspace apps/web && npm run lint --workspace apps/api",
    "prisma:generate": "cd apps/api && npx prisma generate",
    "prisma:migrate:dev": "cd apps/api && npx prisma migrate dev --name init",
    "seed": "cd apps/api && npm run seed"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
